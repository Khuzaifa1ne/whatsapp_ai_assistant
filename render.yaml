services:
- type: web
name: whatsapp-ai-assistant
runtime: python
buildcommand: "pip install -r requirements.txt
startcommand: "gunicorn app:app"
envVars:
  - key: VERIFY-TOKEN
    sync: false
  - key: ACCESS-TOKEN
    sync: false
  - key: PHONE_NUMBER_ID
    sync: false